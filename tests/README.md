# 跨模块测试

根测试目录只保存跨部署单元测试：

- `contract/`：OpenAPI、JSON Schema、SSE 版本和合法/非法黄金样例；
- `e2e/`：使用 Mock Worker 的确定性完整闭环，以及每日/发布候选 GPU 路径入口；
- `fixtures/`：脱敏或合成样例与受控数据集 ID，不保存真实授权原图。

单模块单元测试保存在模块内。测试名称描述行为和期望，不使用序号；所有时间、随机数和外部故障应可注入和复现。
