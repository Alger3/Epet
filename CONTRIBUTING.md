# 贡献指南

## 开始前

1. 阅读 `PLAN.md`、相关模块 README 和已接受 ADR。
2. 确认任务所属阶段与 Gate；Gate 未通过时不得提前扩张下一阶段功能。
3. API、数据库、资源包或事件发生变化时，先修改契约或迁移设计，再修改实现。

## 分支与提交

- 分支：`feat/<scope>-<summary>`、`fix/<scope>-<summary>`、`docs/<summary>`、`chore/<summary>`。
- 提交：遵循 Conventional Commits，例如 `feat(runtime): validate atlas bounds`。
- 一个提交只表达一个可回滚意图；生成文件与其来源文件必须同一提交。
- 不提交密钥、真实照片、模型权重、构建产物、缓存或本地数据库。

## 变更必须同步的内容

| 变更 | 必须同步 |
|---|---|
| API 请求/响应 | OpenAPI、生成类型、契约测试、迁移说明 |
| `.epet` 格式 | JSON Schema、合法/非法样例、加载器测试、兼容策略 |
| 数据库 | 迁移、回滚/兼容说明、数据生命周期 |
| Tauri Command/capability | 权限清单、安全测试、相关 ADR |
| 用户数据处理 | 隐私说明、删除策略、日志/埋点 Schema |
| 运行告警 | 指标定义、阈值、负责人、运行手册 |
| 用户可见行为 | 产品要求、错误状态、E2E、发布说明 |

## 评审清单

- 范围符合当前阶段和冻结决策。
- 正常、错误、重试、取消、离线和恢复路径明确。
- 输入在信任边界处验证，日志已脱敏。
- 有与风险相称的单元、契约、集成或 E2E 测试。
- 没有把 Redis/SSE 当作任务事实来源。
- 没有绕过 Schema、资源归属或幂等约束。
- 文档中的状态、负责人、日期和链接有效。

合并标准见 `docs/development/definition-of-done.md`。
