# 安全策略

## 报告方式

仓库公开前由项目负责人配置私密安全邮箱或代码托管平台的私密漏洞报告功能。本文件不得放置个人邮箱占位值。渠道未配置前，请勿创建包含漏洞细节、令牌、照片或生产地址的公开 Issue。

## 敏感数据

严禁提交：

- 原始或可识别用户照片；
- 设备私钥、刷新凭据、访问令牌和签名 URL；
- 生产对象键、完整本地路径和数据库转储；
- Windows 代码签名凭据与 Tauri 更新签名私钥；
- 未获再分发授权的模型权重或测试素材。

发现疑似泄露时，应先吊销或轮换凭据、阻断访问，再清理历史和发布事故说明。仅从 Git 当前版本删除不等于完成处置。

## 重点安全边界

- 桌宠 WebView 默认无网络、对话框、Shell 和任意文件权限。
- 所有 Tauri Command 校验窗口标签、输入 Schema 和允许路径。
- 所有云端资源同时校验令牌主体与设备归属。
- `.epet` 安装器拒绝路径穿越、符号链接、嵌套包、大小写冲突和资源超限。
- 客户端只加载本地受信内容，不加载任意远程网页。

详细要求见 `docs/architecture/security-boundaries.md`。
